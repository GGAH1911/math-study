# TODO

## 솔버 파라미터화 전수 — 진행 중 (2026-08-14 인계)

**상태**: 4,193개 중 **95개 완료** · 실패 47개(원본 복구됨, 재시도 대상).
상태파일 `db/solutions/_paramstate.json` 으로 **죽은 자리에서 재개**된다.

```bash
python3 scripts/parameterize_solvers.py --workers 8 --model hermes:deepseek/deepseek-v4-flash-0731 --retry-failed
```

### ✅ 모델 선택 결론 — Hermes 경로 채택 (2026-08-14 실측)

**통과율이 문제였던 게 맞았다.** 채팅 API 3/6(50%) → **Hermes 5/6(83%)**. 단발 검증
1건까지 합치면 6/7(86%). 실패 1건도 품질이 아니라 **900초 타임아웃**이라, `PARAM_TIMEOUT`
을 올리면 회수될 여지가 있다. 가설대로 **코드를 돌려 볼 수 있느냐가 갈랐다.**

`scripts/parameterize_solvers.py` 에 `hermes:` 접두 경로를 넣었다(`_run_hermes`). `--model`
에 안 주면 기존 sonnet/Nous 경로는 그대로다 — 크론 영향 없음.

**★TODO 에 적어 뒀던 명령줄은 그대로는 안 돈다.** hermes 0.20.0 에서 밟은 함정 넷:

| 함정 | 실제 | 대응 |
|---|---|---|
| `-m deepseek/...` 단독 | 모델 문자열로 provider 를 **재추론**해 "No LLM provider configured" 로 죽는다 | `--provider custom:nousapi` 를 **반드시 짝지어** 준다 |
| `--yolo` 가 필요한 줄 알았다 | **없어도 승인 없이 파일 생성·python3 실행이 된다** | 안 쓴다. 무승인 권한을 넓힐 이유가 없다 |
| `--in <폴더>` 로 격리 | **격리 안 된다.** 세션 cwd 복원이 이겨 **레포 루트에 `probe.py` 를 만들었다** | `cwd=work` + `--in` + `--no-restore-cwd` 3중 |
| 종료코드로 성공 판정 | **실패해도 0** 을 준다 | 안 믿는다. 판정은 호출부가 두 게이트로 |

마지막 항목 덕에 **모델이 통과했다고 거짓말해도 채택되지 않는다** — 게이트는 호스트가 다시 돈다.

### 💰 비용·시간 — 전수 착수 전에 확인할 것

건당 **추정 $0.0302** (실측 7건 $0.2113 · API 호출 96 · in 169k / **out 337k**).
남은 4,098건 환산 **약 $124**. 채팅 API 추정치($3)보다 **45배 비싸다** — 에이전트 루프가
건당 11~21회를 호출하고 출력 토큰을 48k씩 쓰기 때문이다. 품질을 토큰으로 산 셈.

⚠️ **`actual_cost_usd` 는 전부 0 으로 기록된다**(`~/.hermes/state.db` `session_model_usage`).
위 $124 는 **토큰 단가로 계산한 추정치**일 뿐이다. Nous Portal 이 정액제면 실제 청구는
0 일 수 있다 — **전수 돌리기 전에 요금제를 확인할 것.** 못 쟀으므로 못 쟀다고 적는다.

시간: 워커 3에서 **건당 9.3분**. 워커 8이면 4,098건 ≈ **79시간(3일+)**.

### 🚚 운영 방침 — 소넷 일일 드립 + 필요할 때 버스트 (2026-08-14 확정)

**평시는 소넷이다.** 구독이라 **한계비용 0** 이고, 실측 통과율도 나쁘지 않다(4/4 표본).
DeepSeek 은 통과율이 더 좋아도 건당 $0.03 이 실제로 나간다 — **쿼터가 말랐을 때의 예비
경로**로 남긴다(`--model hermes:deepseek/deepseek-v4-flash-0731`).

크론 **07:00 · `scripts/ops/run_param_daily.sh` · 2시간 박스 · 워커 8**.

```bash
# 여유가 있다고 판단되면 버스트 (수동)
MAX_SECONDS=14400 bash scripts/ops/run_param_daily.sh     # 4시간
```

**왜 건수가 아니라 시간 박스인가**: 난이도가 들쭉날쭉해 건수로 묶으면 어떤 날은 30분,
어떤 날은 4시간이 된다. 쿼터 소모를 예측 가능하게 하려면 시간으로 묶어야 한다.
시간당 59건(워커 8) 기준 2시간이면 하루 ~120건 → 잔여 4,098건에 **약 34일**.

**무인 운영이라 "전부 실패" 로 흐르는 길을 두 겹으로 막았다**:
`looks_quota_exhausted`(쿼터는 자격증명이 **멀쩡한 채로** 오므로 기존 인증검사를 그냥
통과했다) + `--abort-after 6` 연속 실패 회로차단. 안 막으면 남은 수천 건이 전부 실패로
기록돼 실패 목록이 못 쓰게 되고, 다음 재시도가 멀쩡한 문제까지 다시 돈다.

**남은 확인**: ① Nous 요금제(정액제면 DeepSeek 버스트도 공짜) ② `PARAM_TIMEOUT`
1800 으로 타임아웃 실패건이 회수되는지.

## 최상위(평면) 개념 274개를 교육과정 트리로 이전 — 2026-08-14 등록

**무엇**: `docs/concepts/<이름>.md` 처럼 단원 폴더 밖에 놓인 개념 274개.
링크·prerequisites 는 정상이라 **동작에는 문제 없다.** 다만 단원별 개념 목록에 안 나타난다.

**왜 생겼나**: 인제스트의 개념 후보 목록이 비어 있어(죽은 ROOT·비재귀 glob) LLM 이 이름을
지어냈고, `_ensure_concept_exists` 가 그 이름으로 최상위에 파일을 만들었다. 원인은
2026-08-13 에 고쳤다(스코프·게이트·경로 인덱스). 남은 건 이미 쌓인 274개의 자리다.

**목적지 판정 — 단서 3겹**(실측)
| 단서 | 커버 |
|---|---|
| ① 그 개념을 참조하는 **문제들의 unit 다수결**(60%+) | 89개 |
| ② 자신의 `prerequisites` 가 단원 하나를 지목 | 76개 |
| ③ 둘 다 없음 → 본문 읽고 판단 | 나머지 |

①②가 겹치면 상호 검증, 어긋나면 그 건만 사람이 본다.
★**참조하는 문제가 하나도 없는 것이 184개** — 배치보다 먼저 "살릴 개념인가" 를 물어야 한다.
아무 문제도 안 쓰는 개념이면 삭제가 맞을 수 있다.

**함께 고쳐야 하는 참조**: 문제 4,164건의 `concepts:` + 본문 링크 · 다른 개념의
`prerequisites`/`enables` · 위젯/삽화 인덱스.

**절차**: `git mv` 로 이동 → 참조 일괄 치환 → **개념 페이지 전수 200 확인**으로 검증.
①②로 자동 배치되는 것부터(약 120-150개), 나머지는 목록으로 올려 판단받는다.

⚠️ 재매핑 직후에 하지 말 것 — 방금 정리한 참조를 다시 흔든다.

---

## 3D 렌더러 한계 — 3D 기출 72건 전수 조사에서 나온 것 (2026-08-14)

조사 방법: 문제마다 조건을 전부 뽑아 좌표를 세우고 `scripts/ops/sympy_run.py` 로
전수 검증(에이전트 83개). **72건 전부 작도 가능**했고, 조건을 못 맞추는 문제는 0건이었다.
즉 **작도 자체는 막히지 않는다** — 아래는 "그릴 수는 있는데 보기 나쁜" 항목이다.

이번 세션에 고친 것(별도): `assert_on_circle` 2D 인덱싱 · `angle_bisector_dir` ·
헬퍼 6종 추가 · 위치 관계 단정 금지 규칙.

| 항목 | 건수 | 판정 근거 |
|:---|---:|:---|
| `plane` 크기 상한 | 7 | `Geometry3D.tsx` PlaneShape 의 size 상한(10)이 도해 폭보다 작아 평면이 잘려 보인다. 예: 2020 9월모평 가형 29 는 필요 폭 약 18 |
| 은선(HLR) 없음 | 5 | `segment3d.dashed` 가 **고정 플래그**라 회전하면 앞뒤가 뒤집혀도 점선인 채다. 원본 도판의 은선 표현과 어긋난다. 근본 해법은 깊이버퍼 2패스([[project_3d_figure_hlr]]) |
| 라벨 겹침 | 2 | HTML 라벨이 고정 픽셀이라 점이 가까우면 겹쳐 읽힌다(2019 고3 10월 가형 19: 라벨 간 0.127) |
| 납작함 → 축 과장 필요 | 2 | 실좌표 그대로면 밑변 12 대 높이 3 같은 비율이라 안 보인다. 원본 기출도 교과서식으로 높이를 과장한다 |
| `polyhedron` 꼭짓점과 같은 좌표의 `point3d` 조용히 삭제 | 2 | ShapeRouter 가 `console.warn` 만 남기고 버린다 → 라벨이 통째로 사라진다. 우회는 `labels` 배열이나 `text3d` |

**왜 지금 안 고쳤나**: 전부 low 등급이고 작도를 막지 않는다. 반면 `plane` 상한과
HLR 은 렌더러 구조를 건드리는 일이라 별도 작업으로 잡는 게 맞다. 우선순위는
**plane 상한 > point3d 삭제 > 라벨 겹침 > HLR** — 앞의 둘은 한 줄짜리인데
증상이 "안 보인다"라 오해를 크게 부른다.

## 솔버 파라미터화 게이트 — 2026-08-14 **해결됨**

`scripts/ops/verify_solver_params.py` 를 **살아있는 파라미터 ≥2개**(MIN_LIVE)로 조였을 때
2026 고3 7월 46건 중 2건이 걸렸고, 둘 다 원인이 "장식 파라미터"가 아니라 **결합**이었다.
고친 내용은 `scripts/CLAUDE.md` 의 두 규약(값/보기 분리 · VARIANTS)으로 정리했다.

- **공통_08** — 보기 목록이 PARAMS 에 고정돼 있어 계수를 바꾸면 값이 목록 밖으로 나가
  `solve` 가 죽었다. `value(prm)`(값)과 `solve(prm)`(보기 번호)을 나누고 보기를 값에서
  **유도**하게 바꿔 결합을 끊었다. 유도한 보기가 원문제 보기와 같음을 assert 로 고정.
- **공통_22** — 원의 반지름이 자유 파라미터였는데 설계 불변식이 **R² = sx² + sy²** 라
  sx·sy 만 흔들면 교점이 사라지고 `solve` 가 **조용히 None** 을 돌려줬다. 반지름을
  유도값으로 묶고, 해가 없으면 예외를 던지게 했다. 자연수 조건 때문에 파라미터가 서로
  묶여 한 개씩은 못 흔드는 문제라 `VARIANTS` 로 성립하는 조합 5개를 제시한다(답 53·14·31·8·9).
