# 코퍼스 백필 트랙 체크리스트 (A · B · C)

> 박스 / figure / 교정 관련 **코퍼스 백필** 작업을 트랙별로 정리한 체크리스트.
> 완료 시 `- [x]` 로 체크, 진행하며 업데이트한다.
> 최종 갱신: **2026-06-21**

---

## 배경 / 원칙

- 코퍼스 = **문제 4104개**. 교정완료(corrector_done) **943**, `{{FIG}}` 753, `{{TABLE}}` 223.
- 백필이 필요한 이유: extract_figures 전면개선 + 박스 **알고리즘 감지** + corrector 개선이
  이미 처리된(=구 로직) 콘텐츠에 소급 적용되어야 하므로.
- ★**핵심 원칙**: 미교정 ~3161 문제는 **백필 불필요** — 앞으로 교정할 때 현재(신) 로직이 자동 적용된다.
  즉 백필 대상 = **이미 구 로직으로 처리된 것만**.
- **방법별 3트랙**: A=결정적(LLM 0), B=재추출(이미지 재생성·LLM), C=재교정(corrector·LLM).
  - ⚠ **B↔C 커플링**: `extract --apply` 가 searchable_text 를 한 줄로 붕괴시켜 corrector 재실행이 필수.
    그래서 B를 돌리면 C는 같은 패스에서 따라온다(분리 불가).

---

## A. 결정적 백필 (LLM 0 · 교정본 보존 · 읽기전용 · 안전)

박스/표 같은 **구조 정보**를 소스(PDF)에서 결정적으로 뽑아 searchable_text 에 삽입. 재교정 불필요.

- [x] **박스 알고리즘 감지** — `extract_figures.detect_boxes` (PDF 벡터 테두리 = 상하 가로선 + 좌우 세로선)
  - [x] `<보기>` 박스 상단 라벨 2분할(5선) → `cluster_h` 같은 y 가로선 병합
  - [x] 스택 박스 오합침 방지(세로선 미병합)
  - [x] 컬럼 divider · 도형 사각형(한글<2) 제외
  - [x] 시험지 푸터("확인 사항/답안지") 박스 제외
  - [x] **표 박스 제외** (내부 가로 행구분선 = 표 격자 신호 → `{{TABLE}}` 가 테두리째 렌더, 이중방지)
- [x] **`{{BOX}}` 마커 결정적 백필** — `web/scripts/box_backfill.py` (교정 159문제 마킹, partial-skip 0)
  - [x] 줄범위 매핑: 경계(stem아래~질문위) → 마커run((가)(나)·ㄱㄴㄷ·(i)(ii)) → inside 블록
  - [x] 멀티박스(조건+보기) 마커 run 박스순서 1:1 배정
  - [x] `split_crammed` — haiku 가 조건을 한 줄에 cram 한 레거시 결정적 분리(2021수능 나형15류)
  - [x] 한글-only 완화 매칭(PDF `[4점]`→`점?[4]` garble 무시) · stem 가드
- [x] **휴리스틱 폴백 제거** — reconstruct.ts Rule1/2/3(과정이다·조건(가)·보기헤더 추론) 완전 삭제. 박스는 마커로만.
- [x] **ghost `{{TABLE}}` 제거 전수 검증** — 211 `{{TABLE}}` 문제 전부 frontmatter `tables:` 보유, ghost **0** (이미 클린)

### → **A 트랙 완료** ✅

---

## B. 재추출 백필 (extract --apply → 이미지·placeholder 재생성 · 줄붕괴 → 재교정 동반 · LLM)

도형 추출 로직(객체추출·find_region·인라인·캡션)이 바뀌어 **기존 figure 를 재생성**해야 하는 것.

- [x] **범위 측정** (2026-06-21) — 결과:
  - `{{FIG}}` 745문제, figure 파일 **785 png / 1 jpeg** → .png 는 캡쳐와 객체추출-PNG **구분 불가**(임베드 다이어그램은 대개 PNG). 즉 신/구 자동 판별 어려움.
  - has_figure:true 분류: **figures배열 548 / figure_image(구포맷) 153 / 플래그만 17**.
  - 102 회차에 분산. `{{INL}}` 1개뿐.
  - ★**결론**: B는 ~700문제 재추출 = **대규모 LLM 배치**(이미지 재생성 + 줄붕괴 → 재교정). 그런데 **figure 는 현재 다 렌더됨(안 깨짐)**, 사용자는 **원본 PNG**를 보고 재구성 뷰는 **어드민 전용** → B 가치는 어드민/튜터 figure 품질·포맷일관성(사용자 영향 낮음).
- [ ] **`{{INL}}` 인라인 도형 백필** — 현재 1/4104. (재추출 필요)
- [ ] **figure 객체 직접추출 재생성** — 캡쳐 bleed 제거. (재추출 필요)
- [ ] **find_region 페이지버그 영향분** — 샘플 스캔상 ~0건(저순위)
- [ ] **side-caption (R₁/R₂)** — 도형 옆 캡션. (재추출 필요)
- [ ] **구포맷 153(figure_image) → 신포맷({{FIG}}+figures배열) 마이그레이션** — 현재도 렌더됨(구 단일도형 경로), 일관성 작업

> 💡 **권장**: B는 안 깨진 것의 품질개선 + 대규모 LLM 비용 + 어드민 전용 가치 → **명시적 지시 시에만 배치 실행**. (예: 재구성 사용자 노출/아이패드앱 준비 시점)

---

## C. 재교정 백필 (corrector 재실행 · LLM)

corrector 프롬프트/게이트 개선분을 기존 교정본에 소급. **B와 한 패스로 묶임**.

- [x] **선택지 두 줄 중복 제거** — 측정(2026-06-21): 코퍼스 전체 선택지 마커 중복 **0건**(corrector dedup + reconstruct dedup 이 이미 처리). 작업 불필요.
- [ ] **생략기호 ⋮ / ⋯ 방향·위치** — 이미지 대조 필요(vision-dependent)·드문 엣지케이스. 저순위 보류.

### → **C 트랙: 선택지중복 0(완료), 생략기호만 보류** ✅

---

## 확정 사항 (참고)

- **표 박스 4개**(가형_07 · 나형_09 · 나형_11 · 기하_24): `{{TABLE}}` 로 격자·테두리째 렌더되므로 박스 마커 불필요 — **확정 제외**.
- 박스는 **100% 알고리즘**(마커)으로 전환 완료. 텍스트 휴리스틱 0.

---

## 진행 로그

- **2026-06-21** — **A 트랙 완료** ✅: 박스 알고리즘 감지 + `{{BOX}}` 백필 159문제 + 휴리스틱 제거 + 표박스 제외 + split_crammed(나형15 알고리즘 처리) + ghost TABLE 검증(0).
- **2026-06-21** — **B·C 범위 측정 완료**: C 선택지중복 **0건(완료)**, 생략기호만 보류. B는 ~700문제 재추출 = 대규모 LLM·어드민전용 가치 → **명시적 배치 지시 대기**.
- **2026-06-21** — **★교정기 전수 배치 시작**(사용자 지시 "깨끗하지 않아"): ok도 안 깨끗(검증은 줄구조·박스 사각지대) → corrector_done 리셋(930) + 미교정 3160 = **4090 전수**. 파이프라인 = gemma 교정 ∥ sonnet 검증 ∥ agy 재교정(setsid, ~1일, `/tmp/ingest_logs/pipeline_*.log`). **B(figure 재추출)+C(재교정) 동시 수행**(corrector ①단계가 extract --apply).
  - ★감사: corrector 에 우리 수정 반영확인 — 프롬프트(BOX/INL보존·줄바꿈·생략기호·dedup)·extract 감지(박스·표/푸터제외) ✓. **단 박스 견고매핑은 apply_md(단순앵커)에 없어** → apply_md 박스삽입 제거하고 **교정 후 box_backfill(box_range·split_crammed)로** 일괄(전수 완료 후 실행 예정).
  - 전수 완료 후 할 일: ① box_backfill 재실행(견고 박스) ② 결과 커밋 ③ 격리 14 수동검토.
