---
sources: []
created: 2026-05-17
updated: 2026-05-17
concept_type: definition
prerequisites: [docs/concepts/algebra/math-1/지수와_로그.md]
enables: []
mastery: unknown
auto_explained: true
---

# 로그의 덧셈 분해

로그의 덧셈 분해는 두 로그의 합을 하나의 로그로 묶거나, 반대로 하나의 로그를 두 로그의 합으로 나누는 성질이다. 밑이 같은 두 로그에 대해 다음이 성립한다.

$$\log_a MN = \log_a M + \log_a N \quad (a > 0,\ a \neq 1,\ M > 0,\ N > 0)$$

이 성질은 지수법칙 $a^p \cdot a^q = a^{p+q}$ 에서 직접 유도된다. $\log_a M = p$, $\log_a N = q$ 로 놓으면 $MN = a^p \cdot a^q = a^{p+q}$ 이고, 양변에 밑이 $a$ 인 로그를 취하면 $\log_a MN = p + q = \log_a M + \log_a N$ 임을 확인할 수 있다. 곱셈이 덧셈으로, 덧셈이 곱셈으로 바뀐다는 점이 핵심이다.

## 예시

**예 1.** $\log_2 3 + \log_2 4$ 를 간단히 하면

$$\log_2 3 + \log_2 4 = \log_2 (3 \times 4) = \log_2 12$$

**예 2.** $\log_{10} 25 + \log_{10} 4$ 를 계산하면

$$\log_{10} 25 + \log_{10} 4 = \log_{10} (25 \times 4) = \log_{10} 100 = 2$$

두 번째 예시처럼, 겉보기에 구하기 어려운 로그값도 덧셈 분해를 역방향으로 적용해 진수를 합쳐 보면 깔끔한 정수로 떨어지는 경우가 많다. 수능에서 이 역방향 적용이 특히 자주 등장한다.

## 관련 개념

- **로그의 정의** — $\log_a b = c \Leftrightarrow a^c = b$ 라는 기본 정의를 정확히 알아야 덧셈 분해가 왜 성립하는지 이해할 수 있다.
- **지수법칙** — 덧셈 분해의 근거가 되는 $a^p \cdot a^q = a^{p+q}$ 가 선수 지식이다.
- **로그의 뺄셈 분해** — $\log_a \frac{M}{N} = \log_a M - \log_a N$ 으로, 덧셈 분해와 쌍을 이루는 성질이다. 나눗셈이 뺄셈으로 바뀐다.
- **로그의 지수 이동** — $\log_a M^n = n \log_a M$ 으로, 덧셈 분해를 반복 적용한 결과와 같다. $\log_a M^3 = \log_a M + \log_a M + \log_a M = 3\log_a M$.
- **상용로그** — 밑이 10인 로그에서 덧셈 분해를 이용하면 진수를 소인수분해한 뒤 $\log_{10} 2$, $\log_{10} 3$ 등의 근삿값을 조합해 복잡한 값을 계산할 수 있다.
