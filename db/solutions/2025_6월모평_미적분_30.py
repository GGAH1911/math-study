"""
2025 6월모평 미적분 30번
탄젠트의 주기성으로 수열의 점근 거동 분석

조건:
y = √x/10과 y = tan x의 교점 x좌표들: a₁, a₂, ...
극한: (1/π²) × lim(n→∞) aₙ³tan²(aₙ₊₁ - aₙ)

핵심:
1. a₁ = 0, aₙ = cₙ₋₂ (n≥2, cₖ ∈ [kπ, kπ+π/2))
2. cₖ = kπ + π/2 - δₖ로 표현 (δₖ → 0)
3. δₖ ≈ 10/√(kπ) for large k
4. aₙ₊₁ - aₙ ≈ π + (δₙ₋₂ - δₙ₋₁)
5. δₙ₋₂ - δₙ₋₁ ≈ 5/(√π·n^(3/2))
6. tan(aₙ₊₁ - aₙ) ≈ 5/(√π·n^(3/2))
7. aₙ³ ≈ (nπ)³ = n³π³
8. aₙ³tan²(aₙ₊₁ - aₙ) ≈ 25π²
9. (1/π²) × 25π² = 25
"""

import math

def solve():
    # 극한 계산
    # aₙ³ ≈ (nπ)³ = n³π³
    # tan²(aₙ₊₁ - aₙ) ≈ (5/(√π·n^(3/2)))²
    # → aₙ³ × tan²(aₙ₊₁ - aₙ) ≈ n³π³ × 25/(π·n³)
    # → aₙ³ × tan²(aₙ₊₁ - aₙ) ≈ 25π²

    # (1/π²) × 25π² = 25

    return 25

if __name__ == '__main__':
    print(f"답: {solve()}")
