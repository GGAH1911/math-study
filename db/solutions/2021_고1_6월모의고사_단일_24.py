"""
2021 고1 6월모의고사 24번
이차부등식 x² + 8x + (a-6) < 0이 해를 갖지 않는 a의 최솟값

조건: 모든 x에 대해 x² + 8x + (a-6) ≥ 0
판별식: D = 64 - 4(a-6) ≤ 0
64 - 4a + 24 ≤ 0
88 ≤ 4a
a ≥ 22
"""

def solve():
    # x² + 8x + (a-6) < 0이 해를 갖지 않으려면
    # x² + 8x + (a-6) ≥ 0 for all x
    # 판별식 D ≤ 0: 64 - 4(a-6) ≤ 0
    # 64 - 4a + 24 ≤ 0
    # 88 ≤ 4a
    # a ≥ 22

    return 22

if __name__ == '__main__':
    answer = solve()
    print(f"답: {answer}")
