"""
2024 고3 7월모의고사 공통 20번
함수 h(x) = max(f(x), g(x))와 직선 y=k의 교점 조건

주어진 함수:
- f(x) = x³ - 12x
- g(x) = a(x-2) + 2 (직선, (2,2)를 지남)
- h(x) = max(f(x), g(x)) (분기점에서의 더 큰 값)

필요한 계산:
1. f의 극값 구하기:
   f'(x) = 3x² - 12 = 0 → x = ±2
   f(-2) = -8 + 24 = 16 (극대)
   f(2) = 8 - 24 = -16 (극소)

2. h(x)=k가 4개 해를 갖는 조건:
   - h는 f와 g의 max이므로 형태는 "아래 포물선, 위 직선" 형태
   - f(x)=k가 3개 해 → 원래는 3개 교점
   - 이 중 일부가 g(x)=k로 대체되어 총 4개

3. a > 0인 경우: 직선이 올라가므로 h의 형태로는 4개 교점 불가능

4. a < 0인 경우 (직선이 내려갈 때):
   - 조건: 2 - 4a < 16 (특정 기울기 조건)
   - 2 - 4a < 16 → -4a < 14 → a > -3.5
   - 따라서 -3.5 < a < 0

5. 최종 계산:
   m = -3.5, M = 0
   10(M - m) = 10 × 3.5 = 35
"""

def solve():
    # f(x) = x³ - 12x의 극값
    # f'(x) = 3x² - 12 = 0에서 x = ±2
    f_max = 16  # f(-2)
    f_min = -16  # f(2)

    # h(x)=k가 4개 해를 가지려면
    # a < 0이고 2-4a < 16이어야 함
    # 2 - 4a < 16 → a > -3.5

    m = -3.5  # a의 하한
    M = 0     # a의 상한 (a≠0 조건)

    # 답 계산
    result = 10 * (M - m)

    return int(result)

if __name__ == '__main__':
    print(f"답: {solve()}")
