---
// Phase 3 C그룹 — 데이터는 `/api/log`(어드민 전용, API 쪽에서도 막는다).
export const prerender = false;
import BaseLayout from '../layouts/BaseLayout.astro';
import OpsLog from '../components/OpsLog.tsx';
---
<BaseLayout title="작업 로그">
  <div class="max-w-4xl mx-auto px-6 py-8 space-y-6">
    <header>
      <h1 class="text-2xl font-semibold tracking-tight">작업 로그</h1>
    </header>
    <OpsLog client:load />
  </div>
</BaseLayout>
